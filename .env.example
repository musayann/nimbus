# Weather data provider: 'open-meteo' (default, no API key needed) or 'tomorrow-io'
WEATHER_PROVIDER=open-meteo

# Required when WEATHER_PROVIDER=tomorrow-io
# Get your API key at https://app.tomorrow.io/
TOMORROW_IO_API_KEY=
